# {{ project_name }}

## Setup

```bash
uv sync
```

## Usage

```bash
uv run hello
```

## Development

```bash
make sync   # Sync (dev dependencies included)
make fmt    # Format code
make lint   # Run linter
make test   # Run tests
make cov    # Run tests with coverage
```
